server:
  port: 8888

spring:
  application:
    name: cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Chaminda202/microservice-application
          search-paths:
            - 'serive-config'